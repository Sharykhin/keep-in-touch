define(['../../config'],function(config){
    'use strict';

    var moduleConfig = {
        name: config.name + '.Backend',
        templatePath: 'backend/templates/'
    };

    return moduleConfig;
});
