define([
	'../module',
	'../module.config',
	'../../../config'
],function(module, moduleConfig,appConfig){
	'use strict';		
	
	module.controller(moduleConfig.name + '.AuthController',['$scope',function($scope){

			console.log('HERE');

	}]);
});