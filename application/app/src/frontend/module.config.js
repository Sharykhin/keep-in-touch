define(['../../config'],function(config){
    'use strict';
    var moduleConfig = {
        name: config.name + '.Frontend',
        templatePath: 'frontend/templates/'
    };
    return moduleConfig;
});